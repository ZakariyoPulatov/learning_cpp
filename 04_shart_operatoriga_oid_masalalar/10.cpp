// Sana: 2023.01.16
// Muallif: Zakariyo Pulatov
// Maqsad: a va b butun sonlar berilgan. Agar o'zgaruvchilar o'zaro teng bo'lmasa, a va b o'zgaruvchilari ularning yig'indisini o'zlashtrsin.
//         agar teng bo'lsa 0 ni o'zlashtrsin. a va b ning qiymati ekranga chiqarilsin
#include <iostream>

using namespace std;

int main() {

    int a=8, b=2;
    
    if (a!=b) {
        cout << " a = " << a+b << endl;
        cout << " b = " << a+b << endl;

        return 0;
        
    }else (a==b); {
        cout << " a = " << (a=0) << endl;
        cout << " b = " << (b=0) << endl;
    }
        return 0;
}